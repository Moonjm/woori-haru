import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("우리 하루")
                .font(.largeTitle)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
